name: bookswap
description: "A new Flutter project."
publish_to: 'none' # Prevents accidental publishing

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.31.1
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.14.0
  # firebase_storage: ^11.6.4  # REMOVED - Using Cloudinary instead

  # State Management
  flutter_riverpod: ^2.4.9

  # Image Handling
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

  # UI / Formatting
  intl: ^0.18.1

  # Cloudinary (Image Storage)
  http: ^1.1.0
  crypto: ^3.0.3
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Load environment variables
  assets:
    - .env

  # Add assets here if needed
  # assets:
  #   - images/

  # Add fonts here if needed
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
# End of pubspec.yaml